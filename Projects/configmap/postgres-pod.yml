apiVersion: v1
kind: Pod
metadata:
  name: postgres-pod
  labels:
    app: demo-voting-app
    name: postgres-pod
spec:
  containers:
    - name: postgres
      image: postgres:9.4
      envFrom:
      - configMapRef:
          name: postgres-configmap
      ports:
      -  containerPort: 5432
